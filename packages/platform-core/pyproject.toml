[project]
name = "agreement-intelligence-platform"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
  "opentelemetry-api==1.38.0",
  "opentelemetry-exporter-otlp-proto-http==1.38.0",
  "opentelemetry-sdk==1.38.0",
]

[build-system]
requires = ["uv_build==0.12.5"]
build-backend = "uv_build"
